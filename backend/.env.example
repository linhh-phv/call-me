# Local LiveKit (docker-compose --dev) — these are the built-in dev creds.
# For LiveKit Cloud, replace with your project's WS URL / API key / secret.
LIVEKIT_URL=ws://localhost:7880
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret

PORT=3000
