# Used by docker-compose.yml — the IP LiveKit advertises in WebRTC ICE candidates.
# Set this to your Mac's LAN IP so devices on the same Wi-Fi can reach the media port.
# Find it with: ipconfig getifaddr en0
LIVEKIT_NODE_IP=192.168.1.10
